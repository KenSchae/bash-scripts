
# Configuration

## shopt command
```
shopt           # shows all options
shopt autocd    # turns on autocd so you don't need to type cd anymore.
```

## set command
```
set -o          # shows options
```